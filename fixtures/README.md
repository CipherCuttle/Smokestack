# Fixtures

Fixtures must be source-identifiable, deterministic and explicit about whether they are synthetic, qualification, calibration or golden parity fixtures.

Never edit a fixture in place if its identity is already bound into a frozen research or receipt artifact; create a new fixture identity.
