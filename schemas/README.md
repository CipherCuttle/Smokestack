# Schemas

Versioned JSON Schema Draft 2020-12 contracts are introduced in PR-02.

Rules:
- published V1 schema semantics are immutable;
- breaking semantics create V2;
- generated language types are derived artifacts, not schema authority;
- external provider payload schemas are not treated as Smokestack domain schemas.
