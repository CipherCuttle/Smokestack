{
  "name": "smokestack",
  "version": "0.0.0-pr00",
  "private": true,
  "type": "module",
  "description": "Prospective onchain formation and public-attention divergence research system.",
  "engines": {
    "node": "24.19.0",
    "npm": "11.17.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/foundation.test.js dist/test/witness.test.js",
    "check": "npm run typecheck && npm run test",
    "smokestack": "node dist/src/cli.js",
    "witness": "node dist/src/witness/service.js",
    "witness:init": "node dist/src/witness/init.js"
  },
  "devDependencies": {
    "@types/node": "24.10.9",
    "typescript": "6.0.3"
  }
}
